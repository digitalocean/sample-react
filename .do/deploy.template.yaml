spec:
  name: sample-react
  services:
  - build_command: pnpm run build
    environment_slug: node-js
    git:
      branch: pnpm-support
      repo_clone_url: https://github.com/fumblehool/sample-react.git
    name: sample-react
